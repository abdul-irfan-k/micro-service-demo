apiVersion: skaffold/v4beta9
kind: Config
metadata:
  name: micro-service-demo
build:
  artifacts:
    - image: irfan76k/micro-demo-admin
      context: Admin
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
manifests:
  rawYaml:
    - k8s/*
